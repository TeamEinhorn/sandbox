sudo: required

language: java
jdk:
  - oraclejdk8
  
services:
  - docker
  
  
before_install:
  - echo "----- BEFORE_INSTALL -----"
  - chmod +x gradlew

install:
  - echo "----- INSTALL -----"
  - ./gradlew assemble
  - ls ./build/libs
  - docker build -t sandbox .

before_script:
  - echo "----- BEFORE_SCRIPT -----"

script:
  - echo "----- SCRIPT -----"
  - ./gradlew check
